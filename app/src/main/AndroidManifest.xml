<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bakeli.mobilebanking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".HomeActivity"></activity> -->
        <activity
            android:name=".HomeActivity"
            android:label="HomeActivity"
            android:parentActivityName=".LoginActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bakeli.mobilebanking.LoginActivity" />
        </activity>
        <!-- <activity android:name=".InsertUser"></activity> -->
        <activity
            android:name=".InsertUser"
            android:label="InsertUser"
            android:parentActivityName=".LoginActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bakeli.mobilebanking.InsertUser" />
        </activity>
        <activity android:name=".DisplayInfo"></activity>
    </application>

</manifest>